
&После("ПередЗаписью")
Процедура РСК_ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	//++РС Консалт: Минаков А.С.
	Если ЗначениеЗаполнено(ДокументОснование) Тогда
		Дата = ДокументОснование.Дата;
		ДатаВыставления = ДокументОснование.Дата;
		Номер = ДокументОснование.Номер;
		ПредставлениеНомера = Документы.СчетФактураВыданный.ПолучитьПредставлениеНомера(ЭтотОбъект)
	КонецЕсли
	//++РС Консалт: Минаков А.С.
	
КонецПроцедуры
