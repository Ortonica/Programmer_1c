Функция ПолучитьСсылкуНаКодСтатуса(КодСтатуса, ТекстСтатуса = "Успешный код выполнения, сообщение принято на отправку") Экспорт

	спрСтатус = НайтиПоКоду(КодСтатуса);
	
	Если ЗначениеЗаполнено(спрСтатус) Тогда
		Возврат спрСтатус;
	КонецЕсли;
	
	оСпр = СоздатьЭлемент();
	оСпр.Код = КодСтатуса;
	оСпр.ТекстСтатуса = ТекстСтатуса;
	оСпр.Записать();
	
	Возврат оСпр.Ссылка;

КонецФункции // ПолучитьСсылкуНаКодСтатуса()
